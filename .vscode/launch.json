{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Go Server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/server.go",  // Path to your main file
            "env": {},  // Set any environment variables if needed
            "args": [], // Any additional arguments you want to pass
            "showLog": true
        }
    ]
}
